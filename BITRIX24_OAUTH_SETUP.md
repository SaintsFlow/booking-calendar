# Настройка OAuth 2.0 для Bitrix24

## Что реализовано:

### 1. ✅ Поле `bitrix24_user_id` для сотрудников

-   Добавлено поле в модель `User`
-   Добавлено в модальное окно создания/редактирования сотрудника
-   Валидация в контроллере
-   Индекс по `(tenant_id, bitrix24_user_id)` для быстрого поиска

### 2. ✅ OAuth 2.0 авторизация через Bitrix24

**Контроллер:** `App\Http\Controllers\Auth\Bitrix24OAuthController`

**Маршруты:**

-   `GET /auth/bitrix24/redirect` - Перенаправление на Bitrix24
-   `GET /auth/bitrix24/callback` - Обработка callback от Bitrix24

**Модели обновлены:**

-   `Tenant`: добавлены поля `bitrix24_domain` и `bitrix24_member_id`
-   `User`: добавлено поле `bitrix24_user_id`

## Как работает авторизация:

### Шаг 1: Пользователь нажимает "Войти через Bitrix24"

```
GET /auth/bitrix24/redirect
↓
Перенаправление на: https://oauth.bitrix.info/oauth/authorize/?client_id=XXX&response_type=code&state=XXX
```

### Шаг 2: Bitrix24 перенаправляет обратно с кодом

```
GET /auth/bitrix24/callback?code=XXX&domain=company.bitrix24.ru&member_id=XXX&state=XXX
↓
Обмен code на access_token
↓
Получение данных пользователя через API
↓
Поиск/создание тенанта по domain
↓
Поиск/создание пользователя по bitrix24_user_id
↓
Авторизация и редирект на /
```

### Шаг 3: Автоматическое сохранение webhook URL

Webhook URL формируется как:

```
https://{domain}/rest/{member_id}/{access_token}/
```

И автоматически сохраняется в `tenant_bitrix24_settings.webhook_url`

## Логика поиска пользователя:

1. **Поиск по `(tenant_id, bitrix24_user_id)`** - основной метод
2. **Поиск по `(tenant_id, email)`** - если первый не нашёл
3. **Создание нового пользователя** - если ничего не найдено

## Что нужно настроить в .env:

```env
# OAuth credentials теперь хранятся в настройках тенанта (зашифрованно)
# В .env больше ничего не нужно!
```

## ⚠️ ВАЖНО: OAuth credentials хранятся в БД

**CLIENT_ID** и **CLIENT_SECRET** теперь хранятся в зашифрованном виде в таблице `tenant_bitrix24_settings`:

-   `oauth_client_id` (encrypted)
-   `oauth_client_secret` (encrypted)

Настройка происходит через админку: **Настройки → Bitrix24**

## Как получить CLIENT_ID и CLIENT_SECRET:

1. Зайдите в свой Bitrix24 портал
2. Перейдите в **Приложения → Разработчикам → Другое → Локальное приложение**
3. Нажмите **Создать приложение**
4. Заполните:
    - **Название**: Calendar AI
    - **URL вашего обработчика**: `https://ваш-домен.ru/auth/bitrix24/callback`
    - **Права доступа**:
        - `user` - информация о пользователе
        - `crm` - работа с CRM (контакты, сделки)
5. Сохраните и получите:
    - **Код приложения (CLIENT_ID)**: local.XXXXXXXXX.XXXXXXXXX
    - **Ключ приложения (CLIENT_SECRET)**: XXXXXXXXXXXXXXXXXXXXXXXXXX

## Параметры callback от Bitrix24:

```php
// GET /auth/bitrix24/callback
[
    'code' => 'authorization_code',           // Код авторизации
    'domain' => 'company.bitrix24.ru',        // Домен портала
    'member_id' => 'xxxxx',                   // ID установки приложения
    'state' => 'csrf_token',                  // CSRF защита
    'server_domain' => 'bitrix24.ru',         // Домен сервера
]
```

## Обмен code на токены:

```php
POST https://oauth.bitrix.info/oauth/token/
[
    'grant_type' => 'authorization_code',
    'client_id' => env('BITRIX24_CLIENT_ID'),
    'client_secret' => env('BITRIX24_CLIENT_SECRET'),
    'code' => $code,
]

// Ответ:
[
    'access_token' => 'XXXXXXXXXXXX',         // Токен доступа
    'refresh_token' => 'XXXXXXXXXXXX',        // Токен обновления
    'expires_in' => 3600,                     // Время жизни (секунды)
    'scope' => 'user,crm',                    // Права доступа
    'domain' => 'company.bitrix24.ru',        // Домен
    'member_id' => 'xxxxx',                   // ID установки
]
```

## Получение данных пользователя:

```php
GET https://{domain}/rest/user.current.json
Authorization: Bearer {access_token}

// Ответ:
[
    'result' => [
        'ID' => '123',                        // ID пользователя
        'NAME' => 'Иван',                     // Имя
        'LAST_NAME' => 'Иванов',              // Фамилия
        'EMAIL' => 'ivan@company.ru',         // Email
        'WORK_POSITION' => 'Менеджер',        // Должность
    ]
]
```

### Таблица `tenants`:

```sql
- bitrix24_domain (string, nullable, unique) - Домен портала (company.bitrix24.ru)
- bitrix24_member_id (string, nullable) - ID установки приложения
```

## Настройка через админку:

1. Зайдите в **Настройки → Bitrix24**
2. Заполните секцию "Основные настройки":
    - **Включить интеграцию** - чекбокс
    - **Webhook URL** - для REST API (опционально)
    - **OAuth Client ID** - для авторизации пользователей ✨
    - **OAuth Client Secret** - для авторизации пользователей ✨
3. Нажмите **"Сохранить"**

Все чувствительные данные автоматически шифруются при сохранении!

### Таблица `users`:

```sql
- bitrix24_user_id (string, nullable) - ID пользователя в Bitrix24
- INDEX (tenant_id, bitrix24_user_id)
```

```sql
- bitrix24_user_id (string, nullable) - ID пользователя в Bitrix24
- INDEX (tenant_id, bitrix24_user_id)
```

### Таблица `tenant_bitrix24_settings`:

```sql
- webhook_url (text, encrypted) - Webhook URL для REST API
- oauth_client_id (text, encrypted) - Client ID для OAuth 2.0
- oauth_client_secret (text, encrypted) - Client Secret для OAuth 2.0
```

```sql
- bitrix24_domain (string, nullable, unique) - Домен портала (company.bitrix24.ru)
- bitrix24_member_id (string, nullable) - ID установки приложения
```

## Безопасность:

-   ✅ **CSRF защита** через state parameter
-   ✅ **Уникальность доменов** - один tenant на один портал
-   ✅ **Индексы** для быстрого поиска
-   ✅ **Логирование** всех OAuth операций
-   ✅ **Валидация** всех входных данных
-   ✅ **Шифрование credentials** - CLIENT_ID и CLIENT_SECRET хранятся в БД зашифрованно
-   ✅ **Мульти-тенантность** - каждый тенант имеет свои OAuth credentials

## Тестирование:

1. Настройте OAuth credentials в админке (**Настройки → Bitrix24**)
2. Откройте `/auth/bitrix24/redirect?tenant=ваш-домен.bitrix24.ru`
3. Авторизуйтесь на Bitrix24
4. Разрешите доступ приложению
5. Вы будете автоматически авторизованы и перенаправлены на `/`

**Примечание:** Параметр `tenant` должен совпадать с `bitrix24_domain` в таблице `tenants`.

## Логи:

Все операции логируются:

```
🔐 Bitrix24 OAuth callback received
✅ Access token received
👤 User data received
✨ New user created from Bitrix24
✅ User logged in via Bitrix24
```

При ошибках:

```
❌ Invalid OAuth state
❌ Missing required OAuth parameters
❌ Bitrix24 OAuth failed
```

## Что дальше:

1. ~~Настройте `BITRIX24_CLIENT_ID` и `BITRIX24_CLIENT_SECRET` в `.env`~~ **УЖЕ НЕ НУЖНО!**
2. Зайдите в **Настройки → Bitrix24** в админке
3. Заполните поля:
    - **OAuth Client ID**: `local.XXXXXXXXX.XXXXXXXXX`
    - **OAuth Client Secret**: `XXXXXXXXXXXXXXXXXXXXXXXXXX`
4. Сохраните настройки (они будут зашифрованы автоматически)
5. Зарегистрируйте callback URL в Bitrix24 приложении: `https://ваш-домен.ru/auth/bitrix24/callback`
6. Протестируйте авторизацию: `/auth/bitrix24/redirect?tenant=ваш-домен.bitrix24.ru`
